# Base runtime + harness deps. Pinned to the competition ladder version.
# The cabt engine (libcg.so) ships INSIDE this wheel, so this single pin
# gives us the exact same engine the Kaggle ladder runs.
kaggle-environments==1.30.1

numpy>=1.26,<3
pandas>=2.0

# Submission + sample-kernel tooling (Kaggle API)
kaggle>=1.6

# Dev tooling
pytest>=8.0
